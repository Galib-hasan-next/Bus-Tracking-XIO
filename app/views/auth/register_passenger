<?php
// View only. Data is prepared by the controller.
?>

<!DOCTYPE html>
<html>
<head>
    <title>Passenger Registration</title>

    <style>

        *{
            margin:0;
            padding:0;
            box-sizing:border-box;
            font-family:'Segoe UI', sans-serif;
        }

        body{
            min-height:100vh;
            background:#eaf1ff;
            display:flex;
            justify-content:center;
            align-items:center;
            padding:30px;
        }

        .container{
            width:1200px;
            min-height:700px;
            background:white;
            border-radius:25px;
            overflow:hidden;
            display:flex;
            box-shadow:0 20px 50px rgba(0,0,0,0.15);
        }

        /* LEFT SIDE */

        .left{
            width:55%;
            background:
            linear-gradient(
                rgba(0,25,80,.45),
                rgba(0,25,80,.45)
            ),
            url('img/bus.png');

            background-size:cover;
            background-position:center;

            color:white;
            padding:60px;

            display:flex;
            flex-direction:column;
            justify-content:space-between;
        }

        .logo{
            font-size:40px;
            font-weight:bold;
        }

        .title{
            font-size:50px;
            font-weight:bold;
            line-height:1.2;
        }

        .subtitle{
            margin-top:20px;
            font-size:22px;
            line-height:1.6;
        }

        .features{
            width:80%;
            background:rgba(255,255,255,.15);
            padding:25px;
            border-radius:20px;
            backdrop-filter:blur(8px);
        }

        .features p{
            margin:18px 0;
            font-size:18px;
        }

        /* RIGHT SIDE */

        .right{
            width:45%;
            display:flex;
            justify-content:center;
            align-items:center;
            padding:50px;
        }

        .register-box{
            width:100%;
            max-width:400px;
        }

        h1{
            text-align:center;
            color:#0b2b6d;
            margin-bottom:10px;
        }

        .welcome{
            text-align:center;
            color:gray;
            margin-bottom:35px;
        }

        .input-box{
            margin-bottom:18px;
        }

        .input-box input{
            width:100%;
            padding:15px;
            border:1px solid #ddd;
            border-radius:12px;
            font-size:16px;
        }

        .input-box input:focus{
            outline:none;
            border-color:#1d5eff;
        }

        .btn{
            width:100%;
            padding:15px;
            border:none;
            border-radius:12px;
            background:#1d5eff;
            color:white;
            font-size:18px;
            cursor:pointer;
            transition:0.3s;
        }

        .btn:hover{
            background:#0b45d1;
        }

        .error{
            background:#ffe6e6;
            color:red;
            padding:12px;
            border-radius:10px;
            text-align:center;
            margin-bottom:20px;
        }

        .footer{
            text-align:center;
            margin-top:25px;
            color:gray;
        }

        .footer a{
            text-decoration:none;
            color:#1d5eff;
            font-weight:bold;
        }

        @media(max-width:900px){

            .container{
                flex-direction:column;
            }

            .left,
            .right{
                width:100%;
            }

            .left{
                min-height:350px;
            }

            .title{
                font-size:38px;
            }
        }

    </style>
</head>

<body>

<div class="container">

    <!-- LEFT -->

    <div class="left">

        <div class="logo">
            🚌 BUS TRACKER
        </div>

        <div>

            <div class="title">
                Join The Smart Way To Travel
            </div>

            <div class="subtitle">
                Create your passenger account and enjoy
                real-time tracking, online ticket booking,
                and a better travel experience.
            </div>

        </div>

        <div class="features">

            <p>📍 Live Bus Tracking</p>
            <p>🎫 Online Ticket Booking</p>
            <p>⭐ Reviews & Ratings</p>

        </div>

    </div>

    <!-- RIGHT -->

    <div class="right">

        <div class="register-box">

            <h1>Create Account</h1>

            <p class="welcome">
                Register as a Passenger
            </p>

            <?php
            if ($message != "") {
                echo "<div class='error'>$message</div>";
            }
            ?>

            <form method="POST">

                <div class="input-box">
                    <input type="text"
                           name="name"
                           placeholder="Full Name"
                           required>
                </div>

                <div class="input-box">
                    <input type="email"
                           name="email"
                           placeholder="Email Address"
                           required>
                </div>

                <div class="input-box">
                    <input type="text"
                           name="phone"
                           placeholder="Phone Number"
                           required>
                </div>

                <div class="input-box">
                    <input type="password"
                           name="password"
                           placeholder="Password"
                           required>
                </div>

                <div class="input-box">
                    <input type="password"
                           name="confirm_password"
                           placeholder="Confirm Password"
                           required>
                </div>

                <button class="btn" type="submit">
                    Register
                </button>

            </form>

            <div class="footer">
                Already have an account?
                <a href="index.php?page=signin">Sign In</a>
            </div>

        </div>

    </div>

</div>

</body>
</html>

